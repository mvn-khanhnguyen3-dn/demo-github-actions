name: hello-world
on: push
 branches:
      - master
permissions:
  contents: read
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: npm
      - name: Run ESLint
        run: eslint . --ext .js,.jsx,.ts,.tsx,.vue
